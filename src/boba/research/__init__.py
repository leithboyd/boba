"""Research-time analysis utilities (feature-screening gates, uncertainty).

Shared by the per-feature notebooks under `notebooks/features/` so the gate
machinery is defined and TESTED in one place instead of copy-pasted per builder.
Not part of the production ETL path.
"""
